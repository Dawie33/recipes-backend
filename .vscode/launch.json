{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node",
        "request": "launch",
        "name": "start",
        "program": "${workspaceFolder}/src/server.js", 
        "outFiles": [
          "${workspaceFolder}/**/*.js"
        ],
        "envFile": "${workspaceFolder}/.env" 
      }
    ]
  }
  